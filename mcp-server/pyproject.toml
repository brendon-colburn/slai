[project]
name = "slai-mcp-server"
version = "0.1.0"
description = "SLAI - Slay the Spire 2 Learning with AI - MCP Coaching Server"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
